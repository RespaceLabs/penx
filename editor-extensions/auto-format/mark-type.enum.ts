export enum MarkType {
  code = 'code',
  underline = 'underline',
  italic = 'italic',
  bold = 'bold',
  strike_through = 'strike_through',
  highlight = 'highlight',
  subscript = 'subscript',
  superscript = 'superscript',
}
