import { Theme } from '@/theme-helper/types'
import card from '@/themes/card/theme'
import line from '@/themes/line/theme'
import minimal from '@/themes/minimal/theme'

export const themes: Record<string, Theme> = {
  minimal,
  line,
  card,
}
