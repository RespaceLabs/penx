import { Editor } from 'slate'
import { OnSelect } from '@penx/extension-typings'
import { emitter } from './emitter'
import { listSchema } from './listSchema'

export const onSelect: OnSelect = (editor, e) => {}
