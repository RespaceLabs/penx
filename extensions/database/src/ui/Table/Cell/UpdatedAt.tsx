import React, { FC, memo } from 'react'
import { CellProps } from './CellProps'

// eslint-disable-next-line react/display-name
export const UpdatedAtCell: FC<CellProps> = memo((props) => {
  const { cell, selected, editable, updateCell } = props

  return <div></div>
})
