export function getClaimBody(id: string) {
  return `
We will review your pull request as soon as possible.
When the pull request is merged, you can claim the bounty in [/bounties/${id}](https://bounty.penx.io/bounties/${id}).
Thank you for contributing to PenX!
`
}
